module.exports = {
  plugins: [
    // [path, options],
    // path,
    // require('xxx')() -> function
  ],
  snippets: [
    [{ '+1': '👍', dash: '💨' }, { prefix: ':', suffix: ':', disabled: false }],
    [{ '+1': '👍', dash: '💨' }, { prefix: ':', suffix: ':', disabled: false }],
  ]
}
